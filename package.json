{
  "name": "portfolio",
  "version": "2.1.0",
  "description": "Portfolio React/Nestjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky",
    "start:dev": "docker-compose --env-file env/.env.dev  -p 'portfolio-dev'  up -d",
    "release:major": "npm version $(semver $npm_package_version -i major)",
    "release:minor": "npm version $(semver $npm_package_version -i minor)",
    "release:patch": "npm version $(semver $npm_package_version -i patch)"
  },
  "author": "Cedric",
  "license": "ISC",
  "devDependencies": {
    "husky": "^9.0.11"
  },
  "dependencies": {
    "semver": "^7.6.2"
  }
}
